---
import ComparisonLayout from '@components/sections/ComparisonLayout.astro'
import type { Locale } from '@i18n/types'

interface Props { locale: Locale }
const { locale } = Astro.props
---

<ComparisonLayout
  locale={locale}
  comparisonKey="coverManager"
  path="/compare/covermanager/"
/>
