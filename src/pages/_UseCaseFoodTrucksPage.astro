---
import UseCaseLayout from '@components/sections/UseCaseLayout.astro'
import type { Locale } from '@i18n/types'

interface Props { locale: Locale }
const { locale } = Astro.props
---

<UseCaseLayout
  locale={locale}
  useCaseKey="foodTrucks"
  path="/solutions/food-trucks/"
  emoji="🚚"
/>
